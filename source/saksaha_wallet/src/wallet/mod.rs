pub(crate) struct Wallet {
    // db
}

impl Wallet {
    pub fn new() -> Wallet {
        Wallet {}
    }

    pub fn get_balance() {
        // from db
        // get some data
        // return
    }

    pub fn send_tx() {
        // from db
        // check if some data is something
        // return
    }
}
