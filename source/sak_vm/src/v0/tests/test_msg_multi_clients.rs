#[tokio::test(flavor = "multi_thread")]
async fn test_multi_clients_chat() {
    // init();

    // let cli_1;
    // let cli_2;

    let pubkey_1 = {};

    let pubkey_2 = {};

    let shared_secret = {
        // rand gen

        // aes
        // key
    };

    {
        // let msg1 = "blabla";
        // cipher <= fn(key, ..);
        // cipher.encrypt()
        // send msg
        1
    };

    {
        // cipher <= fn(key, ..);
        // let msg2 = cipher.decrypt()

        // read msg
        2
    };

    // check if msg1 == msg2
}
